name: home
publish_to: 'none'

environment:
  sdk: ^3.5.0
  flutter: ">=3.0.1"

dependencies:
  flutter:  
    sdk: flutter
  core:
    path: ../../core
  core_ui:
    path: ../../core_ui
  domain:
    path: ../../domain
  navigation:
    path: ../../navigation
  auto_route: ^7.8.4

dev_dependencies:
  flutter_lints: ^4.0.0
  build_runner: ^2.4.13
  auto_route_generator: ^7.3.2